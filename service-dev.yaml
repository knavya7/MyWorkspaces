apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: cluster-monitoring-dev
  labels:
    app: prometheus
    env: dev
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      name: web
---
apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: cluster-monitoring-dev
  labels:
    app: alertmanager
    env: dev
spec:
  selector:
    app: alertmanager
  ports:
    - port: 9093
      targetPort: 9093
      name: web
---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: cluster-monitoring-dev
  labels:
    app: grafana
    env: dev
spec:
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: 3000
      name: web
